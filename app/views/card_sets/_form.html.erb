<%= form_for(@card_set) do |f| %>
  <% if @card_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@card_set.errors.count, "error") %> prohibited this card_set from being saved:</h2>

      <ul>
      <% @card_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
